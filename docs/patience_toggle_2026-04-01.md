# 安抚机制总开关（2026-04-01）

## 背景

当前安抚机制只支持调整安抚语和间隔，不支持整体关闭。
如果用户不希望在思考期间发送“请稍等”类提示，只能清空 `comfort_words`，表达能力不够直接。

## 本次调整

新增总开关：

- `patience_enabled`

关闭后：

- 不再启动安抚计时器
- 不影响秘书判断、主脑回复、状态切换和锁释放

## 目标

- 允许显式关闭安抚机制
- 保持现有默认行为不变
